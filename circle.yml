general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --pull -t quay.io/markus/rust-lang:beta /home/ubuntu/Dockerfiles
    - docker tag -f quay.io/markus/rust-lang:beta quay.io/markus/rust-lang:1.2-beta

test:
  override:
    - docker images

deployment:
  hub:
    branch: rust-lang-beta
    commands:
      - docker login -e $QUAY_EMAIL -u $QUAY_USER -p $QUAY_PASSWORD quay.io
      - docker push quay.io/markus/rust-lang
