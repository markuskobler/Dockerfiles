general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --pull -t markuskobler/rust-lang:1.0.0 /home/ubuntu/Dockerfiles
    - docker tag -f markuskobler/rust-lang:1.0.0 markuskobler/rust-lang:latest
    - docker tag -f markuskobler/rust-lang:1.0.0 quay.io/markus/rust-lang:1.0.0
    - docker tag -f markuskobler/rust-lang:1.0.0 quay.io/markus/rust-lang:latest

test:
  override:
    - docker images

deployment:
  hub:
    branch: rust-lang
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASSWORD
      - docker push markuskobler/rust-lang
      - docker login -e $QUAY_EMAIL -u $QUAY_USER -p $QUAY_PASSWORD quay.io
      - docker push quay.io/markus/rust-lang
